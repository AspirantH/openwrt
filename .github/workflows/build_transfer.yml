#=================================================
# https://github.com/P3TERX/Actions-OpenWrt
# Description: Build OpenWrt using GitHub Actions
# Lisence: MIT
# Author: P3TERX
# Blog: https://p3terx.com
#=================================================

name: build & transfer

on:
#  schedule:
#    - cron: '0 */8 * * *'
  workflow_dispatch:
  repository_dispatch:
    types: [packages-updated]

env:
  REPO_TOKEN: ${{ secrets.TOKEN_OPENWRT }}
  WECHAT_NOTICE: ${{ secrets.WECHAT_NOTICE }}
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build ${{matrix.target}}
    strategy:
      fail-fast: false
      matrix:
        target: ["${{ github.event.client_payload.target || 'x86_64' }}"]
    steps:
      - name: Checkout
        uses: actions/checkout@main
        with:
          fetch-depth: 0

      - name: Restore last commit SHA cache
        id: cache-commit-restore
        uses: actions/cache/restore@v4
        with:
          path: .commit-cache
          key: commit-cache-${{ matrix.target }}
          restore-keys: commit-cache-${{ matrix.target }}

      - name: Check openwrt-packages commits
        id: check-commit
        run: |
          latest_sha=$(curl -s -H "Authorization: token ${{ secrets.TOKEN_OPENWRT }}" \
            https://api.github.com/repos/AspirantH/openwrt-packages/commits?per_page=1 | \
            jq -r '.[0].sha')
          echo "latest_sha=$latest_sha" >> $GITHUB_ENV
          cached_sha=""
          if [ -f .commit-cache/last_commit.txt ]; then
            cached_sha=$(cat .commit-cache/last_commit.txt)
          fi

          if [ "${{ github.event_name }}" == "workflow_dispatch" ] || [ "${{ github.event_name }}" == "repository_dispatch" ]; then
            echo "Manual or external trigger: forcing build and updating cache."
            mkdir -p .commit-cache
            echo "$latest_sha" > .commit-cache/last_commit.txt
            echo "changed=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          if [ "$latest_sha" == "$cached_sha" ]; then
            echo "No new commit, skipping build."
            exit 0
          else
            echo "New commit detected: $latest_sha (cached: $cached_sha)"
            mkdir -p .commit-cache
            echo "$latest_sha" > .commit-cache/last_commit.txt
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Load Settings.ini
        run: |
          echo ${{matrix.target}}
          source "${{ github.workspace }}/devices/common/settings.ini"
          if [ -f "devices/${{matrix.target}}/settings.ini" ]; then
            source "${{ github.workspace }}/devices/${{matrix.target}}/settings.ini"
          fi
          echo "REPO_URL=${REPO_URL}" >> $GITHUB_ENV
          echo "REPO_BRANCH=${REPO_BRANCH}" >> $GITHUB_ENV
          echo "CONFIG_FILE=${CONFIG_FILE}" >> $GITHUB_ENV
          echo "DIY_SH=${DIY_SH}" >> $GITHUB_ENV
          echo "FREE_UP_DISK=${FREE_UP_DISK}" >> $GITHUB_ENV
          echo "UPLOAD_BIN_DIR_FOR_ARTIFACT=${UPLOAD_BIN_DIR_FOR_ARTIFACT}" >> $GITHUB_ENV
          echo "UPLOAD_FIRMWARE_FOR_ARTIFACT=${UPLOAD_FIRMWARE_FOR_ARTIFACT}" >> $GITHUB_ENV
          echo "UPLOAD_FIRMWARE_FOR_RELEASE=${UPLOAD_FIRMWARE_FOR_RELEASE}" >> $GITHUB_ENV
          echo "UPLOAD_FIRMWARE_TO_COWTRANSFER=${UPLOAD_FIRMWARE_TO_COWTRANSFER}" >> $GITHUB_ENV
          echo "UPLOAD_FIRMWARE_TO_WETRANSFER=${UPLOAD_FIRMWARE_TO_WETRANSFER}" >> $GITHUB_ENV
          sed -i "1a REPO_TOKEN=${{ secrets.TOKEN_OPENWRT }}" ${{ github.workspace }}/devices/common/diy.sh
          sed -i "1a TARGET=${{matrix.target}}" ${{ github.workspace }}/devices/common/diy.sh

      - name: Trigger Packages Update
        continue-on-error: true
        if: contains(github.event.action, 'pkg')
        run: |
          status=$(curl -H "Authorization: token ${{ secrets.TOKEN_OPENWRT }}" -s "https://api.github.com/repos/AspirantH/openwrt-packages/actions/runs" | jq -r '.workflow_runs[0].status')
          if [[ "$status" == "completed" ]]; then
            curl -X POST https://api.github.com/repos/AspirantH/openwrt-packages/dispatches \
              -H "Accept: application/vnd.github.everest-preview+json" \
              -H "Authorization: token ${{ secrets.TOKEN_OPENWRT }}" \
            --data '{"event_type": "update"}'
          fi

      - name: Free disk space
        uses: coder-xiaomo/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Initialization environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install build-essential clang flex bison g++ gawk \
          gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
          python3-setuptools rsync swig unzip zlib1g-dev file wget \
          llvm python3-pyelftools libpython3-dev aria2 jq qemu-utils ccache rename \
          libelf-dev device-tree-compiler libgmp3-dev libmpc-dev libfuse-dev
          sudo -E apt-get -qq autoremove --purge
          sudo -E apt-get -qq clean
          sudo timedatectl set-timezone "Asia/Shanghai"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"

      - name: Get current date
        id: date
        run: |
          echo "date=$(date +'%m/%d_%Y_%H/%M')" >> $GITHUB_ENV
          echo "date2=$(date +'%m/%d %Y')" >> $GITHUB_ENV
          VERSION="$(echo "${{github.event.action}}" | grep -Eo " [0-9.]+" | sed -e 's/ //')" || true
          [ "$VERSION" ] && echo "VERSION=$VERSION" >> $GITHUB_ENV || echo "VERSION=$(date +'%m.%d')" >> $GITHUB_ENV

      - name: Clone source code
        run: |
          set -x
          if [ -n "${{ env.REPO_BRANCH }}" ]; then
            USER_BRANCH="${{ env.REPO_BRANCH }}"
            echo "Using specified branch/tag: $USER_BRANCH"
            git clone $REPO_URL -b "$USER_BRANCH" openwrt
            exit 0
          fi
          if [ "$REPO_URL" == "https://github.com/openwrt/openwrt" ]; then
            echo "Fetching latest release tag from OpenWrt official repository..."
            LATEST_TAG=$(curl -s -H "Authorization: token ${{ secrets.TOKEN_OPENWRT }}" \
              "https://api.github.com/repos/openwrt/openwrt/tags?per_page=1" | jq -r '.[0].name')
            if [ -n "$LATEST_TAG" ] && [ "$LATEST_TAG" != "null" ]; then
              echo "Latest tag found: $LATEST_TAG"
              git clone $REPO_URL -b "$LATEST_TAG" openwrt
              exit 0
            else
              echo "Failed to fetch latest tag, falling back to main branch."
            fi
          fi
          echo "Cloning default branch (main/master) of $REPO_URL"
          git clone $REPO_URL openwrt

      - name: Load custom configuration
        run: |
          function git_clone_path() {
            trap 'rm -rf "$tmpdir"' EXIT
            branch="$1" rurl="$2" mv="$3"
            [[ "$mv" != "mv" ]] && shift 2 || shift 3
            rootdir="$PWD"
            tmpdir="$(mktemp -d)" || exit 1
            if [ ${#branch} -lt 10 ]; then
            git clone -b "$branch" --depth 1 --filter=blob:none --sparse "$rurl" "$tmpdir"
            cd "$tmpdir"
            else
            git clone --filter=blob:none --sparse "$rurl" "$tmpdir"
            cd "$tmpdir"
            git checkout $branch
            fi
            if [ "$?" != 0 ]; then
              echo "error on $rurl"
              exit 1
            fi
            git sparse-checkout init --cone
            git sparse-checkout set $@
            [[ "$mv" != "mv" ]] && cp -rn ./* $rootdir/ || mv -n $@/* $rootdir/$@/
            cd $rootdir
            }
          export -f git_clone_path
          mkdir -p openwrt
          cp -rf devices/common/. openwrt/
          cp -rf devices/${{matrix.target}}/. openwrt/
          cp -rf devices openwrt/
          cd openwrt
          chmod +x devices/common/$DIY_SH
          /bin/bash "devices/common/$DIY_SH"
          cp -f devices/common/$CONFIG_FILE .config
          if [ -f "devices/${{matrix.target}}/$CONFIG_FILE" ]; then
            echo >> .config
            cat devices/${{matrix.target}}/$CONFIG_FILE >> .config
          fi
          if [ -f "devices/${{matrix.target}}/$DIY_SH" ]; then
            chmod +x devices/${{matrix.target}}/$DIY_SH
            echo "/bin/bash devices/${{matrix.target}}/$DIY_SH"
            /bin/bash "devices/${{matrix.target}}/$DIY_SH"
          fi
          cp -Rf ./diy/* ./ || true

      - name: Defconfig
        run: |
          cd openwrt
          make defconfig
          if [[ ! "${{matrix.target}}" =~ (amlogic_meson8b|armsr_armv8|bcm27xx_*|rockchip_armv8|sunxi_*|x86_*|siflower_*) ]]; then
          sed -n '/# Wireless Drivers/,/# end of Wireless Drivers/p' .config | sed -e 's/=m/=n/' >>.config
          sed -i "s/\(kmod-qca.*\)=m/\1=n/" .config
          make defconfig
          fi
          cat .config

      - name: Cache
        uses: stupidloud/cachewrtbuild@main
        with:
          ccache: 'false'
          mixkey: ${{ matrix.target }}
          clean: ${{ contains(github.event.inputs.param, 'nocache') }}
          prefix: ${{ github.workspace }}/openwrt

      - name: Apply custom files
        run: |
          cd openwrt
          mkdir -p files
          cp -rf ../devices/common/diy/package/base-files/files/* files/

      - name: Compile the firmware
        id: compile
        run: |
          shopt -s extglob
          df -hT
          cd openwrt
          sed -i '/define prepare_rootfs/a \	find $(1) -name "*-opkg" -delete' include/rootfs.mk
          sed -i '/define prepare_rootfs/a \[Tab]rm -f $(1)/etc/init.d/led' include/rootfs.mk
          echo -e "$(($(nproc)+1)) thread compile"
          make -j$(($(nproc)+1)) || { log=$(mktemp); make V=s &>$log; cat $log; exit 1; }
          #make -j1 V=s
          rm -rf staging_dir/toolchain-*/bin/*openwrt-linux-musl-lto-dump
          rm -rf staging_dir/toolchain-*/initial
          df -hT

      - name: Organize files
        id: organize
        continue-on-error: true
        run: |
          shopt -s extglob
          cd openwrt/bin/targets/*/*/
          cp $GITHUB_WORKSPACE/openwrt/.config ${{matrix.target}}.config || true
          cp $GITHUB_WORKSPACE/openwrt/build_dir/target-*/linux-*/linux-*/.config ${{matrix.target}}_kernel.config || true
          Emoji=("🎉" "🤞" "✨" "🎁" "🎈" "🎄" "🎨" "💋" "🍓" "🍕" "🍉" "💐" "🌴" "🚀" "🛸" "🗽" "⛅" "🌈" "🔥" "⛄" "🐶" "🏅" "🦄" "🐤")
          echo "EMOJI=${Emoji[$[$RANDOM % ${#Emoji[@]}]]}" >> $GITHUB_ENV
          firmware_file=$(find . -name "*-squashfs-combined-efi.img.gz" | head -n1)
          build_date=$(date +%Y%m%d)
          release_name="${build_date}-openwrt-x86-64-generic-squashfs-combined-efi.img.gz"
          cp "$firmware_file" "$release_name"
          echo "RELEASE_FILE=$(pwd)/$release_name" >> $GITHUB_ENV

      - name: Upload firmware to WebDAV
        id: upload-webdav
        if: env.RELEASE_FILE != ''
        continue-on-error: true
        env:
          WEBDAV_URL: ${{ secrets.WEBDAV_URL }}
          WEBDAV_USER: ${{ secrets.WEBDAV_USER }}
          WEBDAV_PASS: ${{ secrets.WEBDAV_PASS }}
          WEBDAV_PATH: ${{ secrets.WEBDAV_PATH }}
        run: |
          curl -X MKCOL -u "${WEBDAV_USER}:${WEBDAV_PASS}" "${WEBDAV_URL}${WEBDAV_PATH}/" || true
          curl -T "$RELEASE_FILE" -u "${WEBDAV_USER}:${WEBDAV_PASS}" "${WEBDAV_URL}${WEBDAV_PATH}/$(basename "$RELEASE_FILE")"

      - name: Upload to GitHub Release (fallback)
        id: upload-release-fallback
        if: steps.upload-webdav.outcome == 'failure' && env.RELEASE_FILE != ''
        uses: softprops/action-gh-release@master
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN_OPENWRT }}
        with:
          files: "${{ env.RELEASE_FILE }}"
          name: "${{ env.date2 }} ${{matrix.target}} ${{ env.EMOJI }} (fallback)"
          tag_name: "${{ env.date }}_${{matrix.target}}_fallback"
          body: "Fallback release due to WebDAV upload failure."

      - name: WeChat NOTICE
        continue-on-error: true
        if: env.WECHAT_NOTICE
        run: |
          if [[ "${{ steps.compile.outcome }}" == "success" ]]; then
            BUILD_STATUS="编译完成😋"
          else
            BUILD_STATUS="编译失败😂"
          fi

          UPLOAD_STATUS=""
          if [[ "${{ steps.upload-webdav.outcome }}" == "success" ]]; then
            UPLOAD_STATUS="📤 已上传至 WebDAV😋"
          elif [[ "${{ steps.upload-webdav.outcome }}" == "failure" ]]; then
            UPLOAD_STATUS="⚠️ WebDAV 上传失败😂"
            if [[ "${{ steps.upload-release-fallback.outcome }}" == "success" ]]; then
              UPLOAD_STATUS="${UPLOAD_STATUS}，已发布Github😋"
            elif [[ "${{ steps.upload-release-fallback.outcome }}" == "failure" ]]; then
              UPLOAD_STATUS="${UPLOAD_STATUS}，Github发布失败😂"
            fi
          else
            UPLOAD_STATUS="⚠️ 编译失败😂"
          fi

          MSG="📦 OpenWrt 构建报告\n"
          MSG="${MSG}目标平台：${{ matrix.target }}\n"
          MSG="${MSG}固件版本：${{ env.VERSION }}\n"
          MSG="${MSG}构建时间：${{ env.date2 }}\n"
          MSG="${MSG}状态：${BUILD_STATUS}\n"
          MSG="${MSG}上传：${UPLOAD_STATUS}"

          curl -s "https://sctapi.ftqq.com/${{ secrets.WECHAT_NOTICE }}.send?title=OpenWrt 构建通知&desp=$(echo -e "$MSG" | sed 's/\n/%0A/g')"

      - name: Delete workflow runs
        uses: GitRML/delete-workflow-runs@main
        continue-on-error: true
        with:
          token: ${{ secrets.TOKEN_OPENWRT }}
          retain_days: 1
          keep_minimum_runs: 1

      - name: Save commit SHA cache
        if: steps.check-commit.outputs.changed == 'true' && success()
        uses: actions/cache/save@v4
        continue-on-error: true
        with:
          path: .commit-cache
          key: commit-cache-${{ matrix.target }}
